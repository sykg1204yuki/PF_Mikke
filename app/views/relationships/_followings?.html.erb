<% if current_user.followings?(user) %>
  <%= link_to 'フォローをはずす', user_relationships_path(user.id), method: :delete, remote: true, class:"btn btn-outline-primary w-100" %>
<% else %>
  <%= link_to 'フォローする', user_relationships_path(user.id), method: :post, remote: true, class:"btn btn-outline-success w-100" %>
<% end %>